docker {
    name project.property('environment.docker.name') + ':latest'
    files jar.archiveFile
    buildArgs([JAR_FILE: 'rest-house-customer-api.jar'])
    pull true
    noCache true
}
docker.dependsOn(build)